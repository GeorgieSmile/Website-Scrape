# Parse the public Vite JavaScript bundle without executing it.
# Keep the core parser and JavaScript grammar on the compatible 0.25 release line.
tree-sitter==0.25.2
tree-sitter-javascript==0.25.0

# Development check used to keep the single-file scraper consistent.
ruff==0.16.2
pyright==1.1.411
